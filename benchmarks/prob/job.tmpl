#!/bin/bash -x
#COBALT -n 1
#COBALT -q debug-flat-quad
#COBALT -A Performance
#COBALT -t 30

outputfile=$outputfile

st=$$(date '+%s')
echo START TIME:$$st > $outputfile
echo INPUT:"$inpstr" >> $outputfile
clang $tmpfile >> $outputfile
et=$$(date '+%s')
echo END TIME:$$et>> $outputfile
run=$$((et-st))
echo OUTPUT:$$run >> $outputfile
echo


